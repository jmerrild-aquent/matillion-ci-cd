# This is workflow job to execute JSON-compare of matillion objects

name: JSON-compare

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # this is the job that creates the json-difference
  compare:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checout main
        uses: actions/checkout@v4
        with:
          path: main
          
      - name: 'Checkout PR branch'
        uses: actions/checkout@v4
        with:
          ref:  ${{ github.event.pull_request.head.sha }}
          path: pull-request
          
      # Set up Go for jd command funtionality
      - name: Set up GO
        uses: actions/setup-go@v4
        with:
            go-version: '1.20'

      # Install jd command funtionality
      - name: Install json-diff
        run:
         go install github.com/josephburnett/jd@latest
         
      # Gets all diff files
      - name: Initialize diff_files Enviornment variable
        run: |
          {
          echo 'diff_files<<EOF'          
          cd /home/runner/work/${{ github.event.repository.name }}/${{ github.event.repository.name }}/main/ROOT/
          git log --name-only >> diff_files.txt
          grep "^ROOT/" diff_files.txt
          echo EOF
          } >> "$GITHUB_ENV"
          echo $diff_files
      # - name: Create file to manipulate text to achieve a list of files with change
      #   run: |
      #     echo "$diff_files" > diff_files.txt
      #     echo "Initialization of diff_files.txt"
      #     echo $(cat diff_files.txt)
          
      # - name: Manipulate text file to achieve a list of files with change and no extra header rows or incosequential change such as version/workflow and load into BASH variable
      #   run: |
      #     {
      #     echo 'diff_files<<EOF'
      #     start_line=$(grep -n "ROOT" ./diff_files.txt)
      #     end_line=$(grep -c ^ ./diff_files.txt)
      #     end_print_line="$(($end_line-1))"
      #     # "${!var}"
      #     awk 'FNR=="$start_line",FNR=="end_print_line" {print FNR ":" $0}' diff_files.txt
      #     # sed -n "$start_line','$end_print_line'p;'$end_line'q'" diff_files > no_extra_diff_files
      #     echo $(cat ./diff_files.txt)
      #     } >> "#GITHUB_ENV"
      #     # awk 'FNR==10,FNR==20 {print FNR ":" $0}' file.txt
      #     # sed -n '16224,16482p;16483q' filename > newfile
      #     echo "Removed unnecessary rows: $diff_files"
      # - name: Update the enviornment variable and add commas and remove line feeds to the list of files within
      #   run: |
      #     # ${parameter/pattern/string} replacement where:
      #     # parameter is the original string
      #     # pattern is the pattern to find and replace
      #     # string is the replacement value
      #     # intiailize variable
      #     {
      #     files=$diff_files
      #     # Transformation commas
      #     files=${files/".TRANSFORMATION"/".TRANSFORMATION,"}
      #     echo "Update of Transformations in variable files: ${files}"
          
      #     # Orchestration commas
      #     files=${files/".ORCHESTRATION"/".ORCHESTRATION,"}
      #     echo "Update of Orchestrations in variable files: ${files}"

      #     # Remove line feeds
      #     files=${files/"\n"/""} >> "$GITHUB_ENV"
      #     echo "Update of list to remove line feed in variable files: ${files}"

      # # # set s2 credentials 
      # - name: Configure secret
      #   uses: aws-actions/configure-aws-credentials@v2
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: ${{ secrets.AWS_REGION }}
      #     aws-bucket: ${{ secrets.S3_BUCKET_NAME }}
      #     file-path: ./mati-json-diffs/
          
      # Calls json diff action
      - name: Call json-diff
        run: |
          for i in $DIFF_FILES_LIST; do 
            echo ${i}
            b =${i##*/}
            echo ${b}
            DIFF_RESULTS=jd "/home/runner/work/${{ github.event.repository.name }}/${{ github.event.repository.name }}/main/${i}" "/home/runner/work/${{ github.event.repository.name }}/${{ github.event.repository.name }}/pull-request/${i}"
            echo ${DIFF_RESULTS}
          done
          # jd "/home/runner/work/${{ github.event.repository.name }}/${{ github.event.repository.name }}/main/ROOT/Fact Financial Metrics/Fact_Financial_Metrics.ORCHESTRATION" 
          # "/home/runner/work/${{ github.event.repository.name }}/${{ github.event.repository.name }}/pull-request/ROOT/Fact Financial Metrics/Fact_Financial_Metrics.ORCHESTRATION"
      
